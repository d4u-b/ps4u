# ps4u

* setup Linux environment

  `%> cd ~`

  `%> git clone <repo_path>`

  `%> source ps4u/.init.sh`
  
* After setting up Linux environment, we can set-up workarea now

  `%> wa0`
  
  `%> wa`
  
  


